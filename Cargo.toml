[workspace]
resolver = "2"
members = [
  "rocal_macro",
  "rocal_core",
  "rocal_macro_usage",
  "rocal",
  "rocal_cli"
]

[patch.crates-io]
rocal = { path = "rocal" }
rocal-cli = { path = "rocal_cli", optional = true }
rocal-core = { path = "rocal_core" }
rocal-macro = { path = "rocal_macro" }
